

// declare a basePath for this router
import {Router} from "express";

const basePath = '/api/tracker'

// instantiate a new router object
const router = Router()

// define tracker route for the router
router.route('/')
